- name: variables with inventory level
  hosts: localhost
  tasks:
  - name: checking valiables
    ansible.builtin.debug:
      msg: "Hi, I'm learning: {{Course}}, Trainer is: {{Trainer}}, Duration is: {{Duration}} "

